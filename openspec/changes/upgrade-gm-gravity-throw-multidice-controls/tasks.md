## 1. GM 控件改造

- [x] 1.1 将 `GMGravityThrowSandbox.gd` 中旧的 6 个目标面按钮替换为“投掷骰子数”选择控件、6 个目标点数输入框和“投掷骰子”按钮
- [x] 1.2 让 6 个目标点数输入框默认显示 `1`，并使用稳定节点名方便 Debug 测试查找
- [x] 1.3 实现投掷前输入读取与校正：骰子数限制为 1 到 6，目标点数限制为 1 到 6
- [x] 1.4 更新状态文本，显示本次投掷骰子数和实际使用的目标点数，所有玩家可见文本保持中文

## 2. 多骰物理投掷

- [x] 2.1 将单个 `cube_body` 状态改为多骰数组状态，并保证重复投掷会清理上一轮刚体
- [x] 2.2 按选择数量批量创建 `RigidBody3D` 骰子，保留每颗骰子的 6 个面和 21 个可见点数
- [x] 2.3 为多颗骰子设置分散起点、初速度、角速度、地面、边界和骰子间碰撞表现参数
- [x] 2.4 增加只读调试 getter，返回当前刚体数量、每颗骰子的目标点数、点数渲染数量和物理阶段标记

## 3. 落地后目标面校准

- [x] 3.1 实现分段物理感流程：空中下落阶段不接管目标面，记录首次落地接触后才允许进入校准窗口
- [x] 3.2 实现最大演出时长兜底，避免物理长时间不稳定导致 sandbox 卡住
- [x] 3.3 在弹起或滚动阶段冻结并校准每颗骰子的目标点数朝上
- [x] 3.4 当当前姿态距离目标姿态较远时，在校准旋转中加入轻微水平滚动位移
- [x] 3.5 确保投掷完成后不会产生任何战斗点数、骰型、算分、奖励或 `RunState` 修改

## 4. Debug 测试

- [x] 4.1 更新 `DebugGmGravityThrowSmokeTest.gd`，验证 GM 入口和返回流程仍可用
- [x] 4.2 更新测试验证投掷骰子数控件、6 个默认值为 `1` 的点数输入框和“投掷骰子”按钮
- [x] 4.3 增加多骰投掷测试，验证刚体数量等于选择数量且每颗骰子渲染 21 个点数
- [x] 4.4 增加物理感阶段测试，验证空中下落、落地接触、弹起后校准和最终目标点数记录
- [x] 4.5 增加校准滚动测试，验证目标姿态距离较远时发生过轻微水平滚动位移

## 5. 验证

- [x] 5.1 运行 `openspec status --change "upgrade-gm-gravity-throw-multidice-controls"` 确认 artifacts 完整
- [x] 5.2 运行 `res://tests_or_debug/DebugGmGravityThrowSmokeTest.gd`
- [x] 5.3 运行 `res://tests_or_debug/DebugMainStartButtonSmokeTest.gd`
- [x] 5.4 运行主场景启动检查：`--headless --path . --quit-after 3`
- [x] 5.5 如 Godot 命令不可用，在完成说明中记录失败原因和已尝试命令

## 6. 摄像机视角切换

- [x] 6.1 在 `GMGravityThrowSandbox.gd` 中新增摄像机视角按钮，可在上方视角和侧面视角之间切换
- [x] 6.2 保证视角切换只调整 GM sandbox 相机，不修改目标点数、刚体骰子或投掷状态
- [x] 6.3 更新 `DebugGmGravityThrowSmokeTest.gd`，验证默认侧面视角、切到上方视角、再切回侧面视角

## 7. 旧版单骰手感恢复

- [x] 7.1 将 GM 投掷演出恢复为旧版单骰的紧凑分段节奏：落地约 `0.3s`
- [x] 7.2 将触地后的弹起/滚动演出控制在约 `0.5s`，避免等待真实物理导致整体变慢
- [x] 7.3 将目标面预旋控制在弹起早段约 `0.2s`，并保留距离较远时的轻微滚动位移
- [x] 7.4 更新 Debug 测试，验证 `0.3s / 0.5s / 0.2s` 手感约束和约 `0.88s` 总演出时长
